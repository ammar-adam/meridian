# Required — AI pipeline
ANTHROPIC_API_KEY=
PERPLEXITY_API_KEY=

# Optional — leave unset to skip PitchBook (Perplexity covers discover)
PITCHBOOK_API_KEY=

# Model overrides (defaults preserve quality — see lib/api-models.js)
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
PERPLEXITY_RESEARCH_MODEL=sonar-deep-research
PERPLEXITY_SEARCH_MODEL=sonar

# ── Cloud productization (Tier 1) ─────────────────────────────────────────

# Neon Postgres — enables durable shares, teams, cross-device sync
# Create free DB: https://neon.tech → copy connection string
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# Clerk auth — enables sign-in + workspace sync
# Create app: https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# After adding DATABASE_URL, push schema:
#   npm run db:push

# Feature flags
MERIDIAN_ENABLE_SERVER_PDF=true
MERIDIAN_ENABLE_SHARE_LINKS=false

# Server PDF on Vercel — remote Chromium pack (matches @sparticuz/chromium-min version)
CHROMIUM_REMOTE_EXEC_PATH=https://github.com/Sparticuz/chromium/releases/download/v149.0.0/chromium-v149.0.0-pack.x64.tar

# Share links auto-enable when DATABASE_URL is set.
# Server PDF requires Playwright on Railway/Docker — set true there only.
